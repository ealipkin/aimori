;(function ($) {
    'use strict';

    $.widget('{{projectname}}.{{blockname}}', {
        _create: function () {
            this._initCache();
            this._initEvents();
        },
        _initCache: function () {
        },
        _initEvents: function () {
            this._on({

            });
        }
    });
})(jQuery);
